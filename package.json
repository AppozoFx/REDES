{
  "name": "REDES",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:emulators": "cd firebase && firebase emulators:start --only auth,firestore,functions",
    "build:functions": "pnpm --dir firebase/functions run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "turbo": "^2.8.0"
  }
}
