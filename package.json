{
  "name": "REDES",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:emulators": "firebase emulators:start --config ./firebase/firebase.json --project redes-5bb81 --import=./firebase/.emulator-data --export-on-exit=./firebase/.emulator-data",
    "build:functions": "pnpm --dir firebase/functions run build",
    "smoke:api": "node scripts/smoke-api.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "turbo": "^2.8.0"
  }
}
